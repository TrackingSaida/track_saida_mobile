/** Compat: reexporta o padrão de nomes do sistema para telas de operação. */
export {
  formatPersonName as formatMotoboyNome,
  sortByPersonName as sortMotoboysByNome,
  normalizePersonList as normalizeMotoboyList,
} from "../../../utils/personName";
